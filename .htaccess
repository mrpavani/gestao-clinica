<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Optional: Prevent directory listing
    Options -Indexes

    # Basic routing (if we ever switch to pretty URLs later)
    # For now, index.php handles ?page=...
</IfModule>
